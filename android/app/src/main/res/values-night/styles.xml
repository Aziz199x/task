<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme for dark mode. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        
        <!-- Set a static dark status bar -->
        <item name="android:statusBarColor">#1e293b</item>
        <item name="android:windowTranslucentStatus">false</item>
        <!-- Force light icons for the dark background -->
        <item name="android:windowLightStatusBar">false</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <!-- Re-adding android:fitsSystemWindows to prevent blank screen issues -->
        <item name="android:fitsSystemWindows">true</item>
        
        <!-- Ensure the launch screen also has a static dark status bar -->
        <item name="android:statusBarColor">#1e293b</item>
        <item name="android:windowTranslucentStatus">false</item>
        <!-- Force light icons for the dark background -->
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>